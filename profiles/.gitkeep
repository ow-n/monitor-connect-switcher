# Keep the profiles/ folder in the repo even though its contents
# (five.xml + positions.json) are gitignored as machine-specific.
# Generate them on this machine with:  .\monitor-profile.ps1 -Capture five
